# Nusxa oling: cp .env.example .env  va qiymatlarni to'ldiring

PORT=3000

# Tasodifiy uzun matn qo'ying (masalan: openssl rand -hex 32)
SESSION_SECRET=change_this_to_a_long_random_string

# Agar sayt HTTPS orqali ochilsa (masalan Nginx/Cloudflare orqali) true qiling
COOKIE_SECURE=false
